# 备忘：挂账与功能 Backlog

> 本文件是「还没做的事」的单一事实源（2026-09-06 起）。
> **挂账** = 已知债务，多带触发条件；**Backlog** = 尚未开发的功能，按建议优先级分组。
> 完成一项就勾记并注明出处；新想法先记到这里，评审后转正为某期计划（`Docs/plans/`）。

## 一、挂账（债务 / 条件触发）

| 项 | 触发条件 | 备注 |
|---|---|---|
| ~~AI 在玩家无敌帧期间仍发起攻击（空挥）~~ | ✅ 2026-09-06 已修复 | 追击与射手 AI 均已加目标无敌/死亡判断 |
| 受击盒收贴美术剪影（玩家侧宁小勿大） | 正式美术接入 | 需同步重标定 `_attackDistance`（命中可达 = 偏移 + 命中盒半宽 + 目标受击盒半宽） |
| 房间几何 TileMap 迁移 | ✅ 框架已落地（2026-09-06 第四期） | 仅剩正式贴图替换：美术到位后只换 `placeholder_tiles.png` 与图集映射，结构不动 |
| 敌人房间休眠策略 | 世界规模扩大后 | 当前 3×3 窗口内全模拟，测试世界无压力 |
| 关卡异步加载（`LoadThreaded`） | 单房间体量增大后 | 当前同步实例化足够 |
| 脚本目录可选整理 | 顺手时 | ① ScreenShake/CameraRig/Presenter 挪 `Presentation/`（纯外观收益）；② 配置四件套挪 `Config/`（需连带改 `.tres` 脚本路径）——均已评估为低收益暂缓 |

## 二、功能 Backlog

### P0 —— 没有就不算完整游戏

- [ ] **音频系统**：BGM（菜单/游玩）+ 音效（跳/落地/挥击/命中/受击/死亡/重生/UI/接触）。
  架构已预留：逻辑层事件齐全（`Jumped`/`Landed`/`AttackStarted`/`HitConfirmed`/`Damaged`/`Died`），
  挂一个音频编排节点订阅即可，逻辑层零改动
- [x] **死亡反馈**：死亡黑场渐入 +「你死了」提示，重生淡出；两条死亡路径统一走
  `Character.Died`（2026-09-06，音效待音频素材）
- [ ] **出口可视化与目标感**：门占位美术 + 靠近提示（如「↑ 进入」），让「通关去哪」可读
  ——用户裁定：现进出口仅用于测试区块加载，实际游玩不一定有「门」概念，暂缓
- [ ] **正式关卡内容**：3–5 房间的有设计世界（教学 → 战斗 → 捷径/秘密房），复用房间规范
  （边界对齐、Spawn 标记、背景 z_index=-10——见 architecture.md §11）

### P1 —— 玩法深度

- [x] **第二种敌人**（远程射手 + 弹体）：站桩开火、视野触发、玩家无敌期不开火；
  验证了「新增敌人 ≈ 输入源+配置+外观」扩展承诺（2026-09-06 P1 批次，
  另含 Projectile/ProjectileEmitter 可复用弹体基建）
- [x] **敌人死亡反馈**：死亡粒子爆发（Character.Died 驱动，玩家/敌人共用）（2026-09-06 P1 批次）
- [x] **手感打磨包**：命中顿帧（TimeScale 0.05×60ms）+ 受击缩放弹跳（2026-09-06 P1 批次）
- [x] **拾取物**：回血心形拾取（Health.Heal + Pickup，满血不消耗）（2026-09-06 P1 批次）
- [x] **检查点**：触碰更新重生点 + 回满血，激活点亮，随房间缓存保持（2026-09-06 P1 批次）
- [x] **AI 完善**：无敌期不攻击（挂账转正并修复）；巡逻/追击参数已外置导出（2026-09-06）

### P2 —— 完成度

- [x] 手柄支持与按键重映射（2026-09-09 Phase5 完成：默认手柄映射 + 只换键盘事件的重映射；手柄实机验证待人工）
- [x] 设置菜单（2026-09-09 Phase5 完成：全屏/VSync/震屏；音量滑块随音频系统另期）
- [ ] 小地图/世界地图（房间网格数据现成）
- [ ] Game Over 结算页（若引入生命次数机制）
- [ ] 本地化框架（`tr()` 埋点越早越省）
- [ ] 性能预算基线（profiler 跑一次，记录房间数-帧率曲线）

## 三、远期 / 明确暂缓

- 连击/蓄力/远程弹体等战斗扩展（等美术与音效再调手感；弹体基建已就绪）
- 正式美术全量替换（触发：受击盒收紧 + tileset 换皮 两笔挂账）
- P2 完成度批次顺延记录：2026-09-06 评审选定「TileMap 框架先行」，P2 顺延
- 2026-09-09 Phase5 新增挂账：手柄键位自定义与按键图标（glyph）、音频滑块（随音频接入）、
  英文翻译文案（zh_CN 表已就绪）、CI 导出产物 job（可选，默认跳过）、胜利/结局画面、
  导出 exe 启动段错误的排查（见 Phase5 计划执行记录）
- 网络多人、移动端适配、成就系统（未列入任何期）
